import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.colossusbets.testapp',
  appName: 'CB Test',
  webDir: './dist',
  bundledWebRuntime: false
};

export default config;
